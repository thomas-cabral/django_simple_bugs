============
Installation
============

At the command line::

    $ easy_install django_simple_bugs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_simple_bugs
    $ pip install django_simple_bugs